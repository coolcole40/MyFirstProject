var n;
function setup() {
  createCanvas( windowWidth , windowHeight );
  
  fill ( 0 , 255 , 0 , 10 ); 
  noStroke();
  
}
function draw() {
 
 loop(
 n = random(50 , 100);
  ellipse( mouseX , mouseY , n , n );
 )
  

}
